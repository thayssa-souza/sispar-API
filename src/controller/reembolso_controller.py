# Tarefa: implementação da controller com duas rotas:
# visualização de TODOS os reembolsos (GET) => o id_colaborador vai ser padrão, ou seja, não vai ser necessário passar o id_colaborador na URL
# adicionar um reembolso (POST)
# Para enviar multiplos dados para o BD, utilize: db.session.bulk_save_objects(lista[instancias]) /\